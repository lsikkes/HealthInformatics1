<UserControl x:Class="Visualizer.Views.BenchView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="300" d:DesignWidth="300"
             xmlns:local="clr-namespace:Visualizer"
             Margin="{Binding Margin}">
    <Grid>
        <Rectangle   x:Name="rectangle"
                     Width="{Binding BenchRectangleWidth}"
                     Height="100"
                     Fill="Red">
        </Rectangle>
        <Ellipse     x:Name="icon1"
                     Width="80"
                     Height="80"
                     Fill="{Binding IconBrush}">
        </Ellipse>
        <Ellipse     x:Name="icon2"
                     Width="80"
                     Height="80"
                     Fill="{Binding IconBrush}">
        </Ellipse>
        <Ellipse     x:Name="icon3"
                     Width="80"
                     Height="80"
                     Fill="{Binding IconBrush}">
        </Ellipse>
        <Ellipse     x:Name="icon4"
                     Width="80"
                     Height="80"
                     Fill="{Binding IconBrush}">
        </Ellipse>
    </Grid>
</UserControl>